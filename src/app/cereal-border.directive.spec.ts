import { CerealBorderDirective } from './cereal-border.directive';

describe('CerealBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new CerealBorderDirective();
    expect(directive).toBeTruthy();
  });
});
